<div class="searchResults">
    
    {{#each artists}}
    
    <div class="artistsInfo">
        {{#each photo}}
        <img src="{{url}}" alt="">
        {{/each}}
        <h3>Name: {{this.name}}</h3>
        <a href="{{this.href}}">View Albums</a>
    </div>
    {{/each}}
</div>